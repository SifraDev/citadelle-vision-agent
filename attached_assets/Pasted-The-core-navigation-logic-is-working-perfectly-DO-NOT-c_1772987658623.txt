The core navigation logic is working perfectly. DO NOT change the AI logic, the gemini-2.5-flash implementation, or the action parsing. We only need to polish the visual presentation for the demonstration video.

Please update the Playwright interaction code (where injectMarkers and mouse movements occur) with the following visual upgrades:

1. The "Scanner" Markers (Styling upgrade):
Inside injectMarkers, change the ugly red borders to a sleek, high-tech UI look.

Use a semi-transparent blue/teal border (e.g., 2px solid rgba(56, 189, 248, 0.8)).

Give the number badge a dark background with white text, rounded corners, and a modern font (e.g., background: #0f172a; color: #38bdf8; border-radius: 4px; padding: 2px 6px; font-weight: bold; font-family: monospace;).

Add a subtle transparent background to the bounding box (e.g., background: rgba(56, 189, 248, 0.1)).

2. The Visible Ghost Cursor:
Before the loop starts, inject a custom visible cursor into the DOM so the frontend stream actually shows where the mouse is.

JavaScript
await page.addStyleTag({ content: `
  #citadelle-custom-cursor {
    position: absolute; top: 0; left: 0; width: 24px; height: 24px;
    pointer-events: none; z-index: 2147483647;
    transition: transform 0.15s cubic-bezier(0.25, 0.1, 0.25, 1);
    filter: drop-shadow(0 2px 4px rgba(0,0,0,0.5));
  }
`});
await page.evaluate(() => {
  const el = document.createElement("div");
  el.id = "citadelle-custom-cursor";
  // A sleek glowing cursor SVG
  el.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="#38bdf8" stroke="white" stroke-width="2" stroke-linejoin="round"><path d="M5 3L19 12L12 13L9 20L5 3Z"/></svg>';
  document.body.appendChild(el);
});
3. Fluid Mouse Movement:
Update the action execution (for both click and type).
First, move the custom cursor element visually:
await page.evaluate(({x,y}) => { document.getElementById("citadelle-custom-cursor").style.transform = \translate(${x}px, ${y}px)`; }, {x, y});Then, use Playwright's mouse move with steps to make it glide like a human before clicking:await page.mouse.move(x, y, { steps: 25 });     await page.mouse.click(x, y);`